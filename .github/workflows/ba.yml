name: Batch

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo content
        uses: actions/checkout@v3
        with:
          repository: gunnsoo/wa-sample
          path: ./wa-sample
      
      - run: ls ./wa-sample
